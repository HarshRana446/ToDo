typescript
tailwind
vite / webpack
package.json

first input for tasks
then pending task column , then in-progress , done task
this should be drag and drop we can drag the task raw and also the column pending, in-progress, and done.
color vise like border of task name which is pending is red
color vise like border of task name which is pending is yellow
color of done task with green.


in the add task section we should have following fields new popup
name 
due date for tasks
description
mobile responsive 
search tasks

learn the vit.config.ts file with server components and plugins in detail
and learn components.json file and
learn app.jsx and main.tsx 

learn tailwind css, learn the package.json file and
learn the tsconfig.json file
In the task mobile responsive done.


due date bug fix,
learn:-
Git config and its commands
git branch & branch commands
Git init ,Git Repo, Git Status, 
Git Stash, Git Log, Git Merge

Create New branch and push.
Edit button functionality in To-do app
apply the Virtualized list.

Add a Dynamic column like a pending, 
in-progress, done. 

learn and implement the Create,
Update and Delete functionality in Task.

fix the bug in mobile responsive.

